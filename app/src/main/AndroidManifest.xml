<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <uses-feature android:name="android.hardware.camera" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">

        <activity android:name=".CalendarDialog" />

        <meta-data
            android:name="com.google.android.actions"
            android:resource="@xml/network_security_config" />
        <meta-data
            android:name="android.max_aspect"
            android:value="2.1" />

        <activity android:name=".calorie" />
        <activity
            android:name=".TodayExItemLink"
            tools:ignore="Instantiatable" />
        <activity android:name=".SelectItem" />
        <activity
            android:name=".SelectTodayEx"
            tools:ignore="Instantiatable" />

        <activity
            android:name=".TodayExItemRoutine"
            tools:ignore="Instantiatable" /> <!-- <activity android:name=".EditRoutine"></activity> -->
        <!-- <activity android:name=".RoutinItem" /> -->
        <!-- <activity -->
        <!-- android:name=".Routine_recycler" -->
        <!-- tools:ignore="Instantiatable" /> -->
        <activity android:name=".EditLink" />
        <activity android:name=".EnrollExercise" />
        <activity
            android:name=".LoginActivity"
            android:configChanges="orientation"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".record" />
        <activity android:name=".TodayEyeBody" />
        <activity android:name=".TodayExercise" />
        <activity android:name=".TodayDiet" />
        <activity android:name=".SignupActivity"></activity>
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".Enroll"></activity>
    </application>

</manifest>